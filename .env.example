# om-cli API keys — copy to .env and fill in. Never commit .env.
# Keys are read from the environment only; never written to config or transcripts.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: log verbosity for `bun run dev` (debug | info | warn | error | silent)
# OM_LOG_LEVEL=debug
